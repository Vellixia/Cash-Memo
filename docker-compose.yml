services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: cashmemo
      POSTGRES_PASSWORD: cashmemo
      POSTGRES_DB: cashmemo
    ports: ["5432:5432"]
    volumes: [db:/var/lib/postgresql]
volumes:
  db:
