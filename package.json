{
  "name": "cashmemo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": "24.14.0",
    "pnpm": "11.13.1"
  },
  "scripts": {
    "openapi": "cargo run --locked -p cashmemo-api --bin export_openapi -- openapi/cashmemo-v1.json",
    "api:generate": "pnpm openapi && pnpm --dir apps/web orval",
    "api:check": "pnpm api:generate && git diff --exit-code -- openapi/cashmemo-v1.json apps/web/generated/api",
    "verify": "pnpm api:check && cargo fmt --check && cargo clippy -p cashmemo-api --all-targets -- -D warnings && cargo test -p cashmemo-api && pnpm --dir apps/web lint && pnpm --dir apps/web typecheck && pnpm --dir apps/web test --run && pnpm --dir apps/web build",
    "test:operations": "vitest run tests/operations/v1-ci.spec.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "toolchain:check": "node scripts/toolchain/check-versions.mjs"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "vitest": "4.1.10"
  }
}
