{
  "name": "api",
  "private": true,
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo fmt --check && cargo clippy --all-targets -- -D warnings"
  }
}
